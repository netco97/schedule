<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.user.mapper.UserMapper">

    <insert id="insert" parameterType="com.example.demo.user.domain.User">
        INSERT INTO users (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

    <select id="findByUsername" parameterType="String" resultType="com.example.demo.user.domain.User">
        SELECT id, username, password, role
        FROM users
        WHERE username = #{username}
    </select>

</mapper>
